You're going to want to read the "three.js" documentation:

https://devdocs.io/offline


And view the chrome.js api files (in your downloads)
And webknobs




6/22/26
- weird bug where I click on theme and it changes the eyes right when I open 
- Numbers should be little dots 
- Would be cool to have a simplified version, just a grid with important values, color pickers.


- Each of them is minimized at first
- As I change something, a new one opens up. 
- Only have one open at a time. 




5/27/26:
- need to make the paramStore sliders the right color 
- Why is my border in the export just a transparent version of 'secondary'?
- add "Current" color theme, so you can watch the site itself change with you



5/25/26:
- Nearly there, just need to:
    1. Wire in "generated css"
    2. Add some sorting, more options to the palette (like that native os color picker)
    3. Make everything look way better and feel way better


5/24/26:
Your shit is in "claude_and_jonny_food"
Also in "components-ide/NotesToSelf.svelte"





using after high on 5/15:
- love it, love the colors, feeling good & officiala
- should be able to select pixel from image directly via hovering over 
	(do I want to overlay some 'average' bitmap of the pixels? Or just 
		raw pixels? Probably raw pixels, right? The palette gives 
		the functionality of 'average')
- The horizontal bars got off from each other for a bit; separated by 10px
- Having a top 'bar' that is actually formatted nice will be killer
	- should have 'file > (export, save, etc), help'



End of 5/15/26 (pre-visual-overhaul):
Need to:
- Instead of hard-coded "Waiting for Image Upload", we just need some status from phaseStore that says "waiting for [x]" to display on relevent components 
    - Eventually, we eliminate the "focus" outline entirely
- Migrate "Site Settings" to its own svelte component
- Let the bottom two panels scroll down as far as you goddang want 
    - (i.e. instead of clipping the vertical overflow, extend it down the page)
- Add in "Tailored" colors 
- Tweak them with Chroma 
- Add in an export 
- Add in a "view preview" 







5/15/26 pre-visual-overhaul:
Feature audit:

Working:
    - Upload Image
    - Select colors 
    - Tweak webpage settings 
    - View preview

Pending:
    - Tweak colors using chroma.js 
    - "good" design for changing preview params 
    - Export .css definitions 
    - undo / redo tree for "tailored" 



Overhaul visual features:
 - Colors remain black and white
 - Hard borders 
 - Font is defined in .css 
 - 




To do for next time:
- Select a way to tweak the colors in "Tailored" better (click on it, a window opens. Maybe beneath this component?)
- also add a "normal" color picker for "Tailored"
- Wire in a way to affect border size and width

- Export CSS definitions from the preview 
- Save a palette to the desktop  
- Make the swatches trim the returned colors if there were no hits.



End steps:
- See if "swatches" is a real word"
- Make everything's layout much better, more fluid 

Quick points:
- "Randomize" button for the palette
- "Lock" buttons for the palette (works with randomize and select index)
- Text color on palette colors changes based on selected color


Polish:
- Add the 3D renders for colors
- Undo / Redo tree for "tailored"? (size 20)



optional
1. border color 
2. border width
3. border round

Color transformations:
1. apply contrast (change hue)
2. Change color from palette





        Pending:
        - format Palette colors correctly (should show the colored text on background, accents, etc)
        - figure out if "swatches" is a real word
        - Make the auto-convert in the "Palette"
            - as well as the darker/lighter versions of the color
            - that'll be a separate .js file
        - fix the "swatch preview doesn't transition colors" bug 

        - Add the "website preview" section 
        - maybe move the swatch preview between "swatches" and "palette"? 
            - or better yet, just make it do that to the selected one on the palette



        - Parameterize the "Image" uploader
        - add a cube to the "Image" uploader
        - make it sexy



        - Claude made some cool results but the code is stinky. I think our best bet is:
        - chroma.js 
        - three.js 
        - some type of knobs (https://next.jqueryscript.net/svelte/svelte-knob-control/)


        - follow the Web Content Accessibility Guidlines (WCAG)
